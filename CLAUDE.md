# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目性质

这是课程《数学思维实践》（CST4822A）的 **CDIO 二级项目**，不是软件产品。交付物是**实践报告 + 实践过程 + 可复现代码 + 可视化结果**，评分对象是报告与图表，而非工程化的代码库。

- 工作语言：**全中文**（文档、报告、图表标题、代码注释均用中文；与用户沟通也用中文）。
- 小组规模：3 人，分工方案 B″（独立深度执行 + 异步轻协作）。任务二负责人兼统稿。
- 当前状态：任务三代码已建于 `mission3/`（可一键复现，详见 `mission3/README.md`）；任务一/二待建。

## 真理来源（按优先级）

改动任何任务的设计或实现前，先读对应文档：

| 文档 | 作用 |
|---|---|
| `docs/任务书.md` | 官方要求与评分细则（100 分构成），最高优先级 |
| `docs/项目计划.md` | 总体计划、已锁定决策、分工、待定开放问题 |
| `docs/任务一设计.md` | 任务一的详细叙事 + 数学推导 + 代码结构设计（v0.3） |
| `docs/实践报告模板.md` | 报告必须填写的章节结构 |
| `docs/实践过程模板.md` | 过程记录必须填写的表格结构 |
| `docs/*.docx` | 上述文档的 Word 原版，**最终报告用 Word 模板填写** |

冲突时以 `任务书.md`（官方要求）为准；`项目计划.md` 标注 ⚠️ 的条目是**待小组开题会确认的开放问题**，不要擅自定夺。

## 核心架构：三层结构

每个任务**强制**采用统一的「数学模型层 → 计算实现层 → 可视化验证层」表达方式，对应报告的三个小节，也对应评分点。新建任何任务代码或报告章节时都要落齐这三层。

**可视化是评分重点，不是装饰**：每张图必须在报告中说明「展示了什么、观察到什么、如何支持结论」。任务书明确「不能只贴图不分析」，每图要有编号 + 标题 + 文字解释。

## 三个任务与已锁定决策

| 任务 | 学科 | 满分 | 已锁定决策 |
|---|---|---|---|
| 一 | 线性代数 | 15 | 迭代法解 Ax=b；Jacobi + Gauss-Seidel；铁棍(1D)→铁板2×2→铁板30×30 三级递进叙事；稳态热传导场景 |
| 二 | 离散数学 | 15 | 三控开关；逻辑锁定为异或 `L = S1 ⊕ S2 ⊕ S3`；无需开题会再定 |
| 三 | 概率统计 | 15 | Bootstrap 估计；⚠️估计目标（推荐中位数）与数据来源待定 |

## 任务一代码规范（已有详细设计）

代码目录命名为 `missionN/`（与 `项目计划.md` 一致）。任务一（`任务一设计.md` §7）：

```
mission1/
├── heat.py          # 单文件函数化：1D铁棍 + 2D铁板 建模/Jacobi/GS/直接解/画图
├── README.md        # 运行说明
└── output/*.png     # 生成的图
```

函数划分：`build_1d / build_2d / jacobi / gauss_seidel / solve_direct / plot_*`，每个职责单一。

**技术栈（已锁定）**：Python + `numpy` + `matplotlib`（`scipy.sparse` 可选）。停止条件 `‖x_新−x_旧‖∞ < 1e-6` 或达 10000 轮；直接解用 `numpy.linalg.solve` 当裁判。

> 用户全局偏好：「低技术债优先、复用优先」。任务一设计已选**单文件函数化**（非过度工程化的包结构），新任务沿用此风格。
>
> **例外（任务三）**：任务三因 Streamlit 加分项的「双前端」（报告图 `plot_matplotlib.py` + 交互仪表盘 `app.py`），采用「共享计算核心 `bootstrap_core.py` + 双前端 + `experiments.py`/`viz_style.py`/`run_all.py`」的多文件包结构，以复用核心、避免报告图与仪表盘重复代码。此为任务三专属例外（2026-07-04 确认），不推广至任务一/二。

## 代码运行

- 根目录已有统一 `requirements.txt`（`numpy`/`scipy`/`matplotlib` + 任务三的 `pytest` 测试与 `streamlit` 加分仪表盘）；任务一/二仅用前三个。任务三另含 `mission3/tests/` 测试套件：`python -m pytest mission3/tests/`。
- 运行单个任务脚本：`python mission1/heat.py`，图输出到该任务的 `output/` 目录。
- matplotlib 全局统一：中文字体、统一配色、`DPI=300`、合理 `figsize`（见 `项目计划.md` §6，细节待开题会定）。

## 学术规范约束（硬性）

- **AI 工具使用须在报告「参考资料」中说明用途**——本仓库由 Claude Code 协助生成的内容，需如实标注。
- 代码必须**可运行、可复现**；图表必须由真实计算结果生成。
- 报告用自己的语言，不得直接复制他人成果；引用开源代码须注明来源。

## 提交打包

压缩包命名「第x组-2026数学与思维实践项目」，内部按「实践报告 / 实践过程 / 源代码与数据 / 可视化结果」四目录整理。生成报告或代码时，确保能归入这四类之一。
